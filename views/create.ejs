<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Book - Book Library</title>
    <style>
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        input, select { width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px; }
        .btn { 
            padding: 10px 20px !important; 
            border: none !important; 
            border-radius: 4px !important; 
            cursor: pointer !important; 
            text-decoration: none !important; 
            display: inline-block !important; 
            text-align: center !important;
            font-size: 14px !important;
            font-weight: 500 !important;
            transition: all 0.2s ease !important;
            margin: 0 5px !important;
            min-width: 100px;
        }
        .btn-primary { background: #007bff; color: white; }
        .btn-primary:hover { background: #0056b3; }
        .btn-secondary { background: #6c757d; color: white; }
        .btn-secondary:hover { background: #5a6268; }
        .error { color: #dc3545; margin-bottom: 15px; padding: 10px; background: #f8d7da; border-radius: 4px; }
        .form-actions { margin-top: 20px; display: flex; gap: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Add New Book</h2>
        
        <% if (typeof error !== 'undefined') { %>
            <div class="error"><%= error %></div>
        <% } %>

        <form method="POST" action="/books">
            <div class="form-group">
                <label for="isbn">ISBN *</label>
                <input type="text" id="isbn" name="isbn" value="<%= typeof formData !== 'undefined' ? formData.isbn : '' %>" required>
            </div>
            <div class="form-group">
                <label for="title">Title *</label>
                <input type="text" id="title" name="title" value="<%= typeof formData !== 'undefined' ? formData.title : '' %>" required>
            </div>
            <div class="form-group">
                <label for="author">Author *</label>
                <input type="text" id="author" name="author" value="<%= typeof formData !== 'undefined' ? formData.author : '' %>" required>
            </div>
            <div class="form-group">
                <label for="genre">Genre</label>
                <select id="genre" name="genre">
                    <option value="">Select Genre</option>
                    <option value="Fiction" <%= typeof formData !== 'undefined' && formData.genre === 'Fiction' ? 'selected' : '' %>>Fiction</option>
                    <option value="Non-Fiction" <%= typeof formData !== 'undefined' && formData.genre === 'Non-Fiction' ? 'selected' : '' %>>Non-Fiction</option>
                    <option value="Science" <%= typeof formData !== 'undefined' && formData.genre === 'Science' ? 'selected' : '' %>>Science</option>
                    <option value="Technology" <%= typeof formData !== 'undefined' && formData.genre === 'Technology' ? 'selected' : '' %>>Technology</option>
                    <option value="History" <%= typeof formData !== 'undefined' && formData.genre === 'History' ? 'selected' : '' %>>History</option>
                    <option value="Biography" <%= typeof formData !== 'undefined' && formData.genre === 'Biography' ? 'selected' : '' %>>Biography</option>
                </select>
            </div>
            <div class="form-group">
                <label for="year">Publication Year</label>
                <input type="number" id="year" name="year" value="<%= typeof formData !== 'undefined' ? formData.year : '' %>" placeholder="e.g., 2020">
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <input type="text" id="description" name="description" value="<%= typeof formData !== 'undefined' ? formData.description : '' %>" placeholder="Brief description">
            </div>
            
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Add Book</button>
                <a href="/books" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>